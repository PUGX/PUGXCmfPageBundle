{% extends 'PUGXCmfPageBundle::root.html.twig' %}

{% block main_content %}

    <div class="row">
        <div class="col-xs-12">
            <h2>{{ document.title }}</h2>
            <p>
                {{ document.text | raw }}
            </p>
        </div>
    </div>

{% endblock %}
