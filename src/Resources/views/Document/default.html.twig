{% extends 'WebgriffeCmfPageBundle::root.html.twig' %}

{% block main_content %}

    <div class="row">
        <div class="col-xs-12">
            <h2>{{ document.title }}</h2>
            <p>
                {{ document.content }}
            </p>
        </div>
    </div>

{% endblock %}
